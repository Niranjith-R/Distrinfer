This is a WIP project.

The idea is that, If you have a large bulk of data that needs to be run through some models, this is the tool for it. Due to the size of data, Scaling the inference layer would be the best way to increase efficiency. This is the core idea of this project. 